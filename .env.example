# =============================================================================
# Supabase PostgreSQL (producción / Vercel / desarrollo remoto)
# =============================================================================
# App (pooler, puerto 6543) — Settings → Database → Connection string → URI + Transaction mode
DATABASE_URL="postgresql://postgres.cheiwzsgxtssyorsotit:[PASSWORD]@aws-1-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Migraciones Prisma (session pooler, puerto 5432) — Settings → Database → Connection string
DIRECT_URL="postgresql://postgres.cheiwzsgxtssyorsotit:[PASSWORD]@aws-1-us-east-1.pooler.supabase.com:5432/postgres"

# =============================================================================
# SQLite local (solo backup / script migrate:sqlite-to-pg)
# =============================================================================
SQLITE_DATABASE_URL="file:./prisma/dev.db"

# Opcional: habilitar /api/debug-users en producción (NO recomendado)
# ENABLE_DEBUG_API=true
